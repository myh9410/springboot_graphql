type Order {
    no: ID
    member: Member
    order_num: String
    order_date: String
    status: OrderStatus
}

enum OrderStatus {
    CANCEL
    WAIT
    CONFIRM
}

extend type Query {
    getOrderByNo(no: ID): Order!
}