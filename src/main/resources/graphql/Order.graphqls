type Order {
    no: ID
    member: Member
    order_num: String
    order_date: String
    status: OrderStatus
    order_items: [OrderItem]
}

enum OrderStatus {
    CANCEL
    WAIT
    CONFIRM
}

extend type Query {
    getOrderByNo(no: ID): Order!
}